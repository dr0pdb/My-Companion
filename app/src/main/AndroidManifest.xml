<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.srv_twry.studentcompanion">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.READ_SYNC_SETTINGS"/>
    <uses-permission android:name="android.permission.WRITE_SYNC_SETTINGS"/>
    <uses-permission android:name="android.permission.AUTHENTICATE_ACCOUNTS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:enabled="true">
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CodingCalendarListActivity"
            android:label=""
            android:parentActivityName=".HomeActivity"
            android:launchMode="singleTop">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".HomeActivity" />
        </activity>
        <activity android:name=".CodingCalendarContestDetailActivity"
            android:label=""
            android:parentActivityName=".CodingCalendarListActivity"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>

        <provider
            android:name=".Database.DatabaseContentProvider"
            android:authorities="@string/authority"
            android:exported="true"
            android:syncable="true"
            tools:ignore="InnerclassSeparator" />

        <service android:name=".Services.CodingCalendarAuthenticatorService"
            android:exported="true"
            android:process=":auth"
            android:enabled="true"
            tools:ignore="InnerclassSeparator">
            <intent-filter>
                <action android:name="android.accounts.AccountAuthenticator"/>
            </intent-filter>
            <meta-data
                android:name="android.accounts.AccountAuthenticator"
                android:resource="@xml/coding_calendar_authenticator" />
        </service>

        <service
            android:name="com.example.srv_twry.studentcompanion.Services.CodingCalendarSyncService"
            android:exported="true"
            android:process=":sync"
            android:enabled="true"
            tools:ignore="InnerclassSeparator">
            <intent-filter>
                <action android:name="android.content.SyncAdapter"/>
            </intent-filter>
            <meta-data android:name="android.content.SyncAdapter"
                android:resource="@xml/coding_calendar_sync_adapter" />
        </service>

        <receiver android:name=".BroadcastReceivers.ShowSubscribedContestNotificationReceiver"
            tools:ignore="InnerclassSeparator">
        </receiver>

        <receiver android:name=".BroadcastReceivers.DeviceRebootBroadcastReceiver"
            tools:ignore="InnerclassSeparator"
            android:enabled="true"
            android:exported="false"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>